// 12) accept a name and print its length without using in built (strlen) function.

#include<iostream>
using namespace std;

int main() {
    char ch[100];
   // gets(ch);
    int i;
    for (i = 0;i < ch[i] != '\0';i++) {
    }
    cout << "Length of " << ch << " is " << i << endl;
}